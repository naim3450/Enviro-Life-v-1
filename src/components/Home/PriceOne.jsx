import React from 'react'

const PriceOne = ({text,className}) => {
  return (
    <h4 className={`${className} text-orange-600 text-[20px] font-dm font-bold`}>{text}</h4>
  )
}

export default PriceOne